{
  "name": "@visionset/docs-site",
  "private": true,
  "type": "module",
  "description": "Astro + Starlight renderer for docs/. The documentation itself lives in docs/ and is the source of truth.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-docs.mjs",
    "sync:check": "node scripts/sync-docs.mjs --check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.0",
    "mermaid": "^11.16.1",
    "sharp": "^0.35.3",
    "unist-util-visit": "^5.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
